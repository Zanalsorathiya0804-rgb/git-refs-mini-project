# Git Refs Mini-Project (After)

This project now **successfully lists Git references** using `libgit2` for repository management.

## Features:
✅ Lists all Git references in the repository  
✅ Uses `libgit2` for efficient Git reference management  
✅ Improves over the previous placeholder version  

### How to Run:
Make sure you have `libgit2` installed before running the program.

```sh
gcc main.c -o git_refs -lgit2
./git_refs
